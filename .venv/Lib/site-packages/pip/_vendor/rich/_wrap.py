from __future__ import annotations

import re
from typing import Iterable

from ._loop import loop_last
from .cells import cell_len, chop_cells

re_word = re.compile(r"\s*\S+\s*")


def words(text: str) -> Iterable[tuple[int, int, str]]:
    """Yields each word from the text as a tuple
    containing (start_index, end_index, word). A "word" in this context may
    include the actual word and any whitespace to the right.
    """
    position = 0
    word_match = re_word.match(text, position)
    while word_match is not None:
        start, end = word_match.span()
        word = word_match.group(0)
        yield start, end, word
        word_match = re_word.match(text, end)


def divide_line(text: str, width: int, fold: bool = True) -> list[int]:
    """Given a string of text, and a width (measured in cells), return a list
    of cell offsets which the string should be split at in order for it to fit
    within the given width.

    Args:
        text: The text to examine.
        width: The available cell width.
        fold: If True, words longer than `width` will be folded onto a new line.

    Returns:
        A list of indices to break the line at.
    """
    break_positions: list[int] = []  # offsets to insert the breaks at
    append = break_positions.append
    cell_offset = 0
    _cell_len = cell_len

    for start, _end, word in words(text):
        word_length = _cell_len(word.rstrip())
        remaining_space = width - cell_offset
        word_fits_remaining_space = remaining_space >= word_length

        if word_fits_remaining_space:
            # Simplest case - the word fits within the remaining width for this line.
            cell_offset += _cell_len(word)
        else:
            # Not enough space remaining for this word on the current line.
            if word_length > width:
                # The word doesn't fit on any line, so we can't simply
                # place it on the next line...
                if fold:
                    # Fold the word across multiple lines.
                    folded_word = chop_cells(word, width=width)
                    for last, line in loop_last(folded_word):
                        if start:
                            append(start)
                        if last:
                            cell_offset = _cell_len(line)
                        else:
                            start += len(line)
                else:
                    # Folding isn't allowed, so crop the word.
                    if start:
                        append(start)
                    cell_offset = _cell_len(word)
            elif cell_offset and start:
                # The word doesn't fit within the remaining space on the current
                # line, but it *can* fit on to the next (empty) line.
                append(start)
                cell_offset = _cell_len(word)

    return break_positions


if __name__ == "__main__":  # pragma: no cover
    from .console import Console

    console = Console(width=10)
    console.print("12345 abcdefghijklmnopqrstuvwyxzABCDEFGHIJKLMNOPQRSTUVWXYZ 12345")
    print(chop_cells("abcdefghijklmnopqrstuvwxyz", 10))

    console = Console(width=20)
    console.rule()
    console.print("TextualР В РЎвЂ“Р В РЎвЂњР В РІР‚РЋPythonР В РЎвЂ“Р В РЎвЂњР вЂ™Р’В®Р В РІвЂћвЂ“Р вЂ™Р’В«Р вЂ™Р’ВР В РІвЂћвЂ“Р В РІР‚С™Р РЋРЎСџР В РЎвЂ“Р Р†Р вЂљРЎв„ўР РЋРЎвЂєР В РЎвЂ“Р РЋРІР‚СљР Р†Р вЂљРІР‚СњР В РЎвЂ“Р РЋРІР‚СљР В РІР‚С›Р В РЎвЂ“Р Р†Р вЂљРЎв„ўР вЂ™Р’В±Р В РЎвЂ“Р РЋРІР‚СљР РЋР’ВР В РЎвЂ“Р Р†Р вЂљРЎв„ўР вЂ™Р’В·Р В РЎвЂ“Р РЋРІР‚СљР вЂ™Р’В§Р В РЎвЂ“Р РЋРІР‚СљР РЋРІР‚вЂњР В РІвЂћвЂ“Р Р†Р вЂљРІР‚СљР Р†Р вЂљРІвЂћвЂ“Р В Р’В·Р Р†РІР‚С›РЎС›Р РЋРІР‚СњР В РЎвЂ“Р РЋРІР‚СљР Р†Р вЂљРЎС›Р В РЎвЂ“Р РЋРІР‚СљР вЂ™Р’В¬Р В РЎвЂ“Р РЋРІР‚СљР РЋР’ВР В РЎвЂ“Р РЋРІР‚СљР вЂ™Р’В Р В РЎвЂ“Р РЋРІР‚СљР В РІР‚РЋР В РЎвЂ“Р РЋРІР‚СљР РЋР’ВР В РЎвЂ“Р Р†Р вЂљРЎв„ўР В РІР‚РЋР В РЎвЂ“Р В РЎвЂњР вЂ™Р’В§Р В РЎвЂ“Р В РЎвЂњР Р†РІР‚С›РЎС›")

    console.rule()
    console.print("Р В РЎвЂ“Р Р†Р вЂљРЎв„ўР РЋРЎвЂєР В РЎвЂ“Р РЋРІР‚СљР Р†Р вЂљРІР‚СњР В РЎвЂ“Р РЋРІР‚СљР В РІР‚С›Р В РЎвЂ“Р Р†Р вЂљРЎв„ўР вЂ™Р’В±Р В РЎвЂ“Р РЋРІР‚СљР РЋР’ВР В РЎвЂ“Р Р†Р вЂљРЎв„ўР вЂ™Р’В·Р В РЎвЂ“Р РЋРІР‚СљР вЂ™Р’В§Р В РЎвЂ“Р РЋРІР‚СљР РЋРІР‚вЂњР В РЎвЂ“Р В РЎвЂњР В РІР‚РЋ1670Р В РўвЂР РЋРІР‚ВР Р†Р вЂљР Р‹Р В РЎвЂР Р†Р вЂљР’В°Р В РІР‚В Р В РЎвЂ“Р Р†Р вЂљРЎв„ўР Р†Р вЂљРІвЂћСћР В РўвЂР В РІР‚В¦Р РЋРІР‚вЂќР В Р’В·Р Р†Р вЂљРЎСљР В Р С“Р В РЎвЂ“Р В РЎвЂњР вЂ™Р’В§Р В РЎвЂ“Р В РЎвЂњР В Р Р‰")
